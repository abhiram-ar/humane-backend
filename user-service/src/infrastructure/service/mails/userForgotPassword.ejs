<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>Password Reset</title>
      <style>
         body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #1a1a1a;
            background-color: #f4f4f9;
            margin: 0;
            padding: 20px;
         }

         .email-container {
            max-width: 600px;
            margin: 0 auto;
            background: #ffffff;
            border: 1px solid #e0e0e0;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
         }

         .email-header {
            background: linear-gradient(135deg, #abf600, #c8ff65);
            color: #1b1b1b;
            padding: 25px 20px;
            text-align: center;
            border-bottom: 1px solid #dbeea4;
         }

         .email-header h1 {
            margin: 0;
            font-size: 24px;
         }

         .email-body {
            padding: 30px 20px;
            font-size: 16px;
         }

         .email-body h2 {
            margin-top: 0;
            font-size: 18px;
            color: #333;
         }

         .otp {
            display: inline-block;
            margin: 20px 0;
            padding: 12px 18px;
            background-color: #eaffd0;
            font-size: 20px;
            font-weight: bold;
            color: #1b3c00;
            border-radius: 6px;
            letter-spacing: 1px;
         }

         .button {
            display: inline-block;
            margin-top: 25px;
            padding: 12px 24px;
            background-color: #abf600;
            color: #1b1b1b;
            font-weight: bold;
            text-decoration: none;
            border-radius: 6px;
            border: none;
            transition: background-color 0.3s ease;
         }

         .button:hover {
            background-color: #9ddc00;
         }

         .email-footer {
            background-color: #f0f9d2;
            padding: 15px 20px;
            text-align: center;
            font-size: 0.9em;
            color: #4a4a4a;
            border-top: 1px solid #dceea4;
         }
      </style>
   </head>
   <body>
      <div class="email-container">
         <div class="email-header">
            <h1>Password Reset Request</h1>
         </div>
         <div class="email-body">
            <p>
               You recently requested to reset your password for your Humane account. Please use the
               OTP below to complete the process. This code is valid for the next 5 minutes.
            </p>

            <p>
               If you did not request this, you can safely ignore this email or contact our support
               team.
            </p>
            <a
               href="<%= 'http://localhost:5173/auth/recover-password?recoveryToken=' + token %>"
               class="button"
            >
               Reset Your Password
            </a>
         </div>
         <div class="email-footer">
            <p>&copy; <%= new Date().getFullYear() %> Humane. All rights reserved.</p>
         </div>
      </div>
   </body>
</html>
